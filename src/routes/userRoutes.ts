// import { Router } from "express";
// import * as userController from "../controllers/userController";

// const router = Router();

// router.post("/auth/anonymous", userController.anonymousSignIn);
// router.post("/auth/register", userController.register);
// router.post("/auth/login", userController.login);
// router.get("/players/:playerId", userController.getProfile);
// router.put("/players/:playerId", userController.updateProfile);
// router.get("/players/:playerId/statistics", userController.getStatistics);
// router.get("/players/:playerId/achievements", userController.getAchievements);

// export default router;
